# 样板说明（红白版）

本目录是浸没式光刻缺陷指标回收周报的红白配色版。内容与深色蓝图版相同，视觉合同改为纯白底板、标题 `#C00000`（rgb 192,0,0）。

全部数字、lot、工具编号为**演示用虚构数据**。机制名称来自公开文献词汇。

给公司 Agent：锁 `spec_lock.md` 的红白配色/字体，按真实材料重写 `svg_output/`，禁止生图，质量门 0 blocking error 后再导出。不要覆盖深色原版项目。
